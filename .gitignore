# deps & build
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.vite/

# env & secrets —— 永远不要进仓
.env
.env.*
!.env.example

# 本地读码/Agent 交接笔记，不入仓
agent_context.json

# 本地状态 / 会话转储 / 归档
.newide/
*.session.json
my-session.json
*.zip
*.tar.gz
*.log

# memory 本地设计文档（Spec + 代码导读 + 模块导读），不入仓
src/memory/docs/

# editor & OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code 本地文件
**/CLAUDE.md
.claude/

# ZCode 本地计划文件
.zcode/

/.npmrc
