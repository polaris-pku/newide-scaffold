# deps & build
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.vite/
.pnpm-store/
.npm-tmp-cache/

# env & secrets —— 永远不要进仓
.env
.env.*
!.env.example

# 本地读码/Agent 交接笔记，不入仓
agent_context.json

# 本地状态 / 会话转储 / 归档
.newide/
*.session.json
my-session.json
*.zip
*.tar.gz
*.log

# 本地评测/复现产物，不入仓
evalResult/

# memory 本地设计文档（Spec + 代码导读 + 模块导读），不入仓
src/memory/docs/
# memory API 面计划/评审提案（本地设计文档），不入仓
src/memory/api-surface-review-proposal.md
src/memory/memory-api-surface-plan.md

# 本地架构、设计与执行计划文档，不入仓
docs/

# editor & OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code 本地文件
**/CLAUDE.md
.claude/

# ZCode 本地计划文件
.zcode/

/.npmrc

# AI 驱动测试生成残留 — 脚手架 / 报告 / 子项目
DL_RESULT.md
jwt-auth-system/

# 本地复现/调试脚本，不入仓
scripts/repro-*.ts


*[^ -~]*
docs/

# 角色分歧实验：从本地运行归档回收的问题陈述（派生数据，不入仓）
eval/data/*.recovered.jsonl
eval/data/*.provenance.json

# 本地临时脚本 / 派生数据（金标提取、离线核验脚本等），不入仓
temp/
