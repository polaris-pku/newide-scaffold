# F 方向埋点清单（RFC §1 记忆纵向 + §2 长程协调 + §3 Council 互决策消融 + §4 端到端打榜）

> **执行分层（2026-07-18）**：全文仍是 RFC §1–§4 **归因层**完整清单；近期若目标仅为「能跑出评测结果」，只验收下文「结果层」，其余章节保留为 roadmap，**不作为出分阻塞项**。

## 共用定义

**埋点**：凡是为 F 方向指标提供**原始信号**的记录点（Harness 输出、`EventStore` 事件、嵌套字段、持久化实体等），**不限于** `event_type`。

| 层级           | 含义                                                              |
| -------------- | ----------------------------------------------------------------- |
| **L1 Harness** | F 组评测外层输出（不要求 scaffold 落盘）                          |
| **L2 Event**   | `EventStore.appendEvent()` 独立事件                               |
| **L3 Field**   | 嵌在 L2 payload、对象实体或 Spec 存储里的字段（不必单独成 event） |

**L2 信封**（实现以 scaffold 为准；C Spec 另增可选 `subject_type`）：

`event_id`, `event_type`, `subject_id`, `run_id`, `task_id`, `payload`, `created_at`, `schema_version`

**依据**：

- F 方向 RFC §1–§2（评测指标与实验设计）
- §1 字段：`方向B：Agent角色与记忆系统——Spec`
- §2 事件与对象：`方向C：协调层 状态管理 与 council Spec`
- 实现锚点：`src/telemetry/`（用法见同目录 `README.md`）；评测管线 `eval/`

**观测出口**：F 方向信号写入 `TelemetrySink`（默认 `NoopTelemetrySink`）。C 侧 EventStore 事件经 `RuntimeOrchestrator.appendEvent` 自动 mirror；B 侧 memory-cycle 经 `recordMemoryCycleTelemetry` 观测；L1 由外部 Harness 经 `FHarnessTelemetryPort` 写入。**不替代** B/C 的 EventStore emit 与实体持久化。

**命名边界**：

- §1 的 B Spec 是**实体字段 / 数据流契约**，不是 EventStore 事件名表；`memory.*`、`metrics.updated` 属于 F 评测 / scaffold 的**拟议观测事件名**。
- `memory.ablation` 属于 F Harness 实验配置键，用于承载 RFC §1.2 的 B0 / B1 / B2 / B3 消融条件；不是 B Spec 字段。
- 若 B 方向正式补充事件契约，应优先以 B 事件契约为准；当前清单只约束 F 方向需要观察哪些原始信号。

---

## 0. 结果层 vs 归因层（执行优先级）

| 层级                  | 目标                                       | 验收标准                                                                                                                                         | 与本文关系                               |
| --------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- |
| **结果层（近期 P0）** | 固定子集上跑通评测并写出可复现分数         | `predictions.jsonl` + `summary.json`；L1 含 resolved/applied/f2p/p2p（或等价 Pass 判定）；`run-meta` 含 `run_id` / `instance_id` / mode / subset | **只要求本节与 L1 harness 字段**         |
| **归因层（roadmap）** | §1–§4 消融、resume、Council 白盒、打榜对照 | 见正文 §1–§4 与各章「v0 缺口」                                                                                                                   | **不阻塞出分**；adapter/catalog 可先备着 |

### 0.1 结果层最小埋点（出分必达）

| 信号                      | 形态                                                                                            | 说明                                                     |
| ------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| SWE-EVO（或等价）判卷字段 | L1：`instance_id`, `resolved`, `fix_rate`/`applied`, `f2p_*`, `p2p_regression`                  | **出分本体**；经 `eval/` 写入，真跑依赖 SWE-EVO + Docker |
| Run 元数据                | `run_id`, `instance_id`, subset, prediction `mode`, 可选 `memory.ablation` / `scaffold_variant` | `eval/` → `run-meta.json` / `dataset-manifest.json`      |
| 评测产物                  | `predictions.jsonl`, `summary.json`；（推荐）`telemetry.jsonl`                                  | 约定见 `eval/README.md`；JSONL 非硬门槛                  |

**可选（有则更好，无则仍算出分）**：`proxy.llm_usage_recorded`（仅当需要 Tokens/成本）；memory-cycle 主链 mirror（仅当需要解释记忆路径）。

### 0.2 结果层交付条件（工程验收）

| 条件                                                   | 说明                                                  |
| ------------------------------------------------------ | ----------------------------------------------------- |
| 默认分支含 `eval/` 与 `eval:*` scripts                 | 可复现交付入口                                        |
| `v0-smoke`：stub →（可选）oracle → 稳定 `summary.json` | 出分冒烟；oracle 需 SWE-EVO + Docker                  |
| 不作为结果层阻塞                                       | §2/§3 全量 emit、Verified 打榜、Proxy 全量 Fair-Setup |

### 0.3 归因层明确推迟（不挡出分）

| 块          | 典型内容                                                                  | 处理                                              |
| ----------- | ------------------------------------------------------------------------- | ------------------------------------------------- |
| §2          | resume / message / lease / kill / Checkpoint 七项 / CooperBench           | 标 P2/blocked，等 C 业务                          |
| §3          | `council.*` 全链、`run_audit.json`、Decision Packet、拓扑消融             | MockCouncil 未升级前禁止宣称「可做 Council 消融」 |
| §1 细字段   | `top_k_scores`、confidence、persona、agent_lifecycle、完整 `AgentMetrics` | 正式消融前再补                                    |
| §4 打榜工程 | Verified 30 case 主控、仿真组①②、Fair-Setup 全审计、跨 case 报表          | 与「smoke/dev 出分」解耦，另排期                  |

---

# 一、§1 持久化角色价值度量（记忆纵向）

> C Spec §5.4.6：Checkpoint **不保存** B 方向长期记忆；§1 埋点与 C 层分离观测。

## 1.1 评测问题与实验配置

**问题**：Experience / Skills / Persona 持久化能否提升 SWE-EVO 跨实例工程表现？各层贡献多少？

| 配置键            | 取值               | 来源                          |
| ----------------- | ------------------ | ----------------------------- |
| `memory.ablation` | B0 / B1 / B2 / B3  | F Harness 约定；承载 RFC §1.2 |
| `instance_seq`    | SWE-EVO 跨实例序号 | F Harness 约定；RFC §1.3      |
| Harness           | SWE-EVO（主）      | RFC §1.3                      |

## 1.2 指标 → 埋点

| 指标                          | 原始信号                                                                                                                                                             |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Resolved / Fix / Applied rate | L1：`instance_id`, `f2p_*`, `p2p_regression`, `applied`                                                                                                              |
| B0→B1                         | L1 + `memory.ablation` + L3：`DriverReturn.referenced_experiences`、`ExtractResult`；可由拟议 L2 `memory.experience_referenced` / `memory.extraction_completed` 承载 |
| B1→B2                         | L1 + L3：`SkillRecord.review_status` / `ExtractResult.skills_promoted`；可由拟议 L2 `memory.skill_promoted` 承载                                                     |
| B2→B3                         | L1 + L3：`PersonaDef.version` / §4.4 触发条件；可由拟议 L2 `memory.persona_updated` 承载                                                                             |
| B0→B3（headline）             | L1 汇总；不替代分步归因                                                                                                                                              |
| token_per_fix（§2 P1-B 共用） | L1 Proxy 或 L3：`AgentMetrics.token_cost_total`；可由拟议 L2 `metrics.updated` 承载                                                                                  |

## 1.3 埋点清单

### L1 — Harness

| 埋点位置         | 字段                                                                                        | 触发          |
| ---------------- | ------------------------------------------------------------------------------------------- | ------------- |
| SWE-EVO evaluate | `instance_id`, `instance_seq`, `resolved`, `fix_rate`, `f2p_*`, `applied`, `p2p_regression` | 实例结束      |
| Proxy            | `input_tokens`, `output_tokens`, `case_id`                                                  | 每次 LLM 调用 |

### L2 — EventStore（Direction B / F 观测）

> 下表的 `memory.*` / `metrics.updated` 是 F 方向为评测归因提出的 EventStore 观测名；B Spec 当前只定义实体字段、数据流和少量 Hook 名，不定义这些 EventStore 事件名。

| event_type                     | 状态                                                                                                          | 触发                    | payload 要点                                                                | B Spec 对齐点                                                              |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------- | ----------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `memory.context_pack_built`    | **Telemetry ✓**（memory-cycle）；EventStore demo 部分（仅 `role_id`/`memory_refs`）                           | ContextPack / 检索完成  | `role_id`, `ablation`, `retrieved_*_ids[]`, `top_k_scores[]`                | B Spec §5.1 只覆盖检索/载荷分离；事件名来自 scaffold                       |
| `driver.run_result`            | **Telemetry ✓**（memory-cycle 含 L3 `driver_return`）；EventStore demo 部分（无 `driver_return`）             | Driver 结束             | `status`, `artifact_refs`；L3 `driver_return`                               | §3.4 `DriverReturn`                                                        |
| `memory.experience_referenced` | **Telemetry ✓**（memory-cycle，当 `referenced_experiences` 非空）                                             | 引用经验                | `referenced_experiences[]`: `{experience_id, applied, effectiveness, note}` | §3.4 `referenced_experiences`；§4.3 `effectiveness`                        |
| `buffer.report_received`       | **Telemetry ✓**（memory-cycle）                                                                               | DriverReturn 入 pending | `task_id`, `source_driver`, `buffer_seq`, `extraction_status`               | §3.5 `BufferSnapshot`                                                      |
| `memory.extraction_triggered`  | **Telemetry 部分**（memory-cycle；`trigger=immediate`，MVP 同步非真实门控）                                   | 缓冲区门控              | `trigger`, `pending_count`                                                  | §4.2 容量/时间/优先级门控                                                  |
| `memory.extraction_completed`  | **Telemetry ✓**（memory-cycle）                                                                               | 萃取批次结束            | `ExtractResult` 全字段                                                      | §3.9 `ExtractResult`；§6.6 Hook                                            |
| `memory.confidence_updated`    | F adapter 就绪；**未接线**                                                                                    | 置信度回写              | `experience_id`, `old/new_confidence`, `confidence_history[]`               | §3.3 `ExperienceRecord.confidence_history`；§4.3 置信度规则                |
| `memory.skill_promoted`        | **Telemetry ✓**（memory-cycle，晋升成功时）                                                                   | 晋升 Skill              | `experience_id`, `skill_id`, `review_status`                                | §3.2 `SkillRecord.review_status`；§4.3 晋升管道；§6.6 Hook                 |
| `memory.persona_updated`       | F adapter 就绪；**未接线**                                                                                    | Persona 归纳            | `persona_version`, `trigger_reason`                                         | §3.1 `PersonaDef.version`；§4.4 触发条件（`trigger_reason` 为 F 观测字段） |
| `metrics.updated`              | **Telemetry 部分**（memory-cycle；`skill_count`/`experience_count`/`persona_version`，无 `token_cost_total`） | 任务结束                | `AgentMetrics` 快照                                                         | §3.8 `AgentMetrics`                                                        |
| `memory.agent_lifecycle`       | F adapter 就绪；**未接线**                                                                                    | respawn / retire        | `action`, `trigger`                                                         | §3.6 `AgentHandle`；退休字段待补                                           |

`effectiveness`：`fully_effective` | `partially_effective` | `ineffective` | `not_applicable`（B Spec §4.3）。

### L3 — 嵌套字段

| 记录点        | 位置               | 字段                                                             |
| ------------- | ------------------ | ---------------------------------------------------------------- |
| Driver 六字段 | `driver_return`    | `decisions`, `blockers`, `assumptions`, `referenced_experiences` |
| Buffer 元数据 | `buffer_meta.json` | `pending_count`, `total_processed`, `total_dead_letters`         |

## 1.4 v0 缺口（§1）

> 更新基准：`src/telemetry/` 观测层已接入（orchestrator mirror + memory-cycle observer + harness port）。下表区分 **EventStore 事实** 与 **TelemetrySink 观测**。

| 状态                                      | 内容                                                                                                                                                                                                                                                                                             |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Telemetry 已覆盖（memory-cycle 主链）** | `memory.context_pack_built`（含 `ablation`/`retrieved_*_ids`，无 `top_k_scores`）、`driver.run_result`（含 L3 `driver_return`）、`memory.experience_referenced`（条件）、`buffer.report_received`、`memory.extraction_completed`、`memory.skill_promoted`（条件）、`metrics.updated`（部分字段） |
| **Telemetry 部分**                        | `memory.extraction_triggered`（MVP 同步处理，`trigger=immediate`，非 B §4.2 真实门控）；`metrics.updated`（缺 `token_cost_total` 等完整 `AgentMetrics`）                                                                                                                                         |
| **EventStore demo 部分**                  | `basic-flow` 仍 emit `memory.context_pack_built` / `driver.run_result`，payload 较清单缺 `ablation`、`retrieved_*_ids`、`driver_return`                                                                                                                                                          |
| **F adapter 就绪、未接线**                | `memory.confidence_updated`、`memory.persona_updated`、`memory.agent_lifecycle`                                                                                                                                                                                                                  |
| **L3 未观测**                             | `buffer_meta`（`pending_count` 等）；`top_k_scores`                                                                                                                                                                                                                                              |
| **L1**                                    | `FHarnessTelemetryPort` + builder 就绪（SWE-EVO / CooperBench / Proxy）；**scaffold 内无 Harness 执行**，需外部评测框架调用                                                                                                                                                                      |
| **B 业务（非 F 缺口，供对照）**           | B 存储与 memory-cycle 已实现（Repository / Buffer / 提取 / 晋升）；**B 正式 EventStore 事件名仍未冻结**，memory-cycle 路径不写 EventStore                                                                                                                                                        |
| **需补契约**                              | B 方向 EventStore 事件名；`memory.*` 与 B Spec `agent.*` Hook 的命名关系；`trigger_reason` 等 F 观测字段                                                                                                                                                                                         |

---

# 二、§2 长程自主协调能力

## 2.1 评测问题与实验配置

| 配置键          | 取值                      | RFC  |
| --------------- | ------------------------- | ---- |
| `coord.profile` | D00 / D01 / D10 / D11     | §2.1 |
| `eval.phase`    | P1-A / P1-B / P2-A / P2-B | §2.2 |

**D10**：有 checkpoint/resume，**禁** claim / message / lease。  
**D01**：有 P2P 协调，**禁** `_coord.state.checkpoint` / `resume`。

**观测边界**：P2 resume 只恢复 C 层（Checkpoint / ResumePackage）；**不恢复** Driver `model_context` 与 B 记忆（C Spec §5.1）。

## 2.2 指标 → 埋点

| 指标                               | 实验 | 原始信号（C 事件 + L3）                                                         |
| ---------------------------------- | ---- | ------------------------------------------------------------------------------- |
| both_passed / coordination_deficit | P1-A | L1 CooperBench + L2 `agent.message_send`                                        |
| Fix / Resolved / token_per_fix     | P1-B | L1 SWE-EVO + Proxy                                                              |
| resume_success_rate（K=5）         | P2   | L2 `task.checkpoint_resume` → `task.started` + L3 `ResumePackage` + tool（K=5） |
| resume_fidelity                    | P2   | L3：kill 前 `Checkpoint` vs resume 后 `ResumePackage`（§2.6）                   |
| duplicate_work_rate（K=5）         | P2   | L3：`semantic_handoff` + resume 后 tool                                         |
| ΔFix_rate                          | P2   | L1 分段 + L2 **`eval.agent_crash`**（F 扩展）                                   |
| resume_after_multi_agent_block     | P2-B | L2 `task.started` / status + L3 `MessageDelivery` + `FileLease`                 |
| Fix rate（扰动后）                 | P2   | L1 SWE-EVO                                                                      |

## 2.3 C Spec 事件映射（§4.4.12）

> **实现与验收以 C Spec 事件名为准**；右列为 F 评测 / scaffold 别名或补充。

| C Spec `event_type`（必 emit） | 触发 API / 时机                            | F 评测用途       | scaffold v0           | D10 禁？   |
| ------------------------------ | ------------------------------------------ | ---------------- | --------------------- | ---------- |
| `task.created`                 | `_coord.task.create`                       | 任务起点         | ✓                     | —          |
| `task.claimed`                 | `_coord.task.claim`                        | P1-A 协调        | ✗                     | ✓          |
| `task.started`                 | claim/resume 后继续                        | resume_success   | ✗                     | —          |
| `task.completed`               | 任务完成                                   | 生命周期         | ✓（demo）             | —          |
| `task.failed`                  | 任务失败                                   | 生命周期         | ✗                     | —          |
| `task.checkpoint_resume`       | `_coord.state.resume` **之前**             | resume 链核心    | ✗                     | D01/D00 禁 |
| `task.before_merge`            | 合并前 Hook                                | P2-B kill 临界态 | ✗（仅有 gate demo）   | —          |
| `agent.message_send`           | `_coord.message.send`                      | P1-A / fidelity  | ✗                     | ✓          |
| `agent.message_recv`           | 消息投递到达                               | 协调 trace       | ✗                     | ✓          |
| `agent.checkpoint`             | `_coord.state.checkpoint`                  | CP 持久化        | ≈`checkpoint.saved` ✓ | D01/D00 禁 |
| `system.timeout`               | `_coord.message.timeout` / WaitingSet 超时 | P2-B blocked     | ✗                     | —          |
| `system.budget_exceeded`       | 预算耗尽                                   | 长程预算         | ✗                     | —          |
| `lifecycle.human_gate`         | 人工门禁                                   | defer/ask 路径   | ✗                     | —          |
| `council.decision`             | Council 轮次结束                           | §2 **可忽略**    | ✓                     | —          |

**F 评测扩展**（RFC §2.3 专用，C Spec 未定义；`eval.*` 前缀建议实现时采用）：

| event_type          | 触发                       | 用途                                               | scaffold v0                              |
| ------------------- | -------------------------- | -------------------------------------------------- | ---------------------------------------- |
| `eval.agent_crash`  | P2 Kill（SIGTERM/SIGKILL） | ΔFix_rate 分段；对应 RFC `system.agent_crash` 语义 | F builder + port ✓；**运行时未自动触发** |
| `eval.cold_restart` | D00/D01 新会话 + 4k 摘要   | P2 对照基线                                        | F builder + port ✓；**运行时未自动触发** |

**非 C 冻结、F 可选辅助**：

| event_type            | 说明                                                          |
| --------------------- | ------------------------------------------------------------- |
| `task.status_changed` | `{from, to}` 审计用；C 优先用离散事件（`task.claimed` 等）    |
| `gate.result`         | v0 有；C 持久化为 `GateResultRecord`（`_gate.report_result`） |

**Resume 事件链（D10/D11，合并 RFC + C）**：

```text
eval.agent_crash
  → _coord.state.resume
  → emit task.checkpoint_resume          ← C Spec 硬性要求
  → 生成 ResumePackage（L3）
  → emit task.started（若 next_action = continue）   ← RFC §2.3.1
  → K 轮内 driver tool（L3）
```

`interrupt.resume` 仅外部 CI 事件（RFC §2.3.1），**不**用于 driver crash。

## 2.4 埋点清单 — L1 Harness

| 埋点位置             | 字段                                                                           | 触发                           |
| -------------------- | ------------------------------------------------------------------------------ | ------------------------------ |
| SWE-EVO evaluate     | 同 §1 L1                                                                       | P1-B、P2                       |
| CooperBench evaluate | `both_passed`, `coordination_deficit`, `failure_taxonomy`                      | P1-A                           |
| Proxy                | 同 §1 L1                                                                       | token_per_fix                  |
| P2 扰动主控          | `kill_at`, `progress_pct`, `tool_call_count_at_kill`, `checkpoint_id_at_kill?` | 与 `eval.agent_crash` 交叉校验 |

## 2.5 埋点清单 — L2 EventStore（C Spec 准绳）

| event_type                       | payload 要点（对齐 C Spec）                                                         | C 章节         |
| -------------------------------- | ----------------------------------------------------------------------------------- | -------------- |
| `task.created`                   | `TaskCreateRequest` 摘要：`spec`, `role_id?`, `parent_task_id?`, `deps?`, `budget?` | §4.4.2         |
| `task.claimed`                   | `task_id`, `agent_id` → status `claimed`                                            | §4.4 API       |
| `task.started`                   | `task_id`, `run_id?`, `source`: resume \| claim \| create                           | RFC §2.3.1     |
| `task.completed` / `task.failed` | `task_id`, `reason?`                                                                | §4.4.12        |
| `task.checkpoint_resume`         | `task_id`, `checkpoint_id`, `restore_policy`, `on_invalid`                          | §5.4.8         |
| `task.before_merge`              | `task_id`, `artifact_refs[]`, `gate_result_refs[]`                                  | §4.4.12        |
| `agent.message_send`             | `message_id`, `thread_id`, `type`, `from_agent_id`, `requires_ack`                  | §4.4.6         |
| `agent.message_recv`             | `message_id`, `recipient_agent_id`, `delivery_id`                                   | §4.4.6         |
| `agent.checkpoint`               | `checkpoint_id`, `checkpoint_type`, `trigger`, `parent_checkpoint_id?`；详见 L3     | §5.4.6         |
| `system.timeout`                 | `delivery_id?`, `waiting_set_id?`, `task_id`                                        | §7.4           |
| `system.budget_exceeded`         | `task_id`, `budget_field`                                                           | §4.4 API       |
| `lifecycle.human_gate`           | `gate_point`, `attempt_id`                                                          | defer/ask 路径 |
| `eval.agent_crash`               | `progress_pct`, `tool_call_count`, `had_checkpoint`, `kill_at_status`               | RFC §2.3.1     |
| `eval.cold_restart`              | `summary_token_count`（≤4k）, `summary_fields[]`                                    | RFC §2.3.3     |

**C 持久化对象（L2 事件 + L3 实体并用）**：

| 对象                 | 写入时机              | F 评测字段                                                          |
| -------------------- | --------------------- | ------------------------------------------------------------------- |
| `GateResultRecord`   | `_gate.report_result` | `decision`, `gate_point`, `target_state`                            |
| `MergeAuthorization` | 合并授权              | `authorized`, `source`, `artifact_refs`, `target_branch`（§4.4.13） |
| `MessageDelivery`    | send/ack/timeout      | `status`, `ack_at`, 未 ack 计数 → fidelity                          |
| `FileLease`          | acquire/release       | `holder_id`, `path_glob`, `status` → P2-B                           |

**Checkpoint `trigger` 枚举**（C §5.4.6）：`manual` | `periodic` | `shutdown` | `blocked` | `escalated` | `gate_result` | `timeout` | `budget_exceeded`。

**TaskStatus**（C v0 必持，§4.4）：`created` | `claimed` | `running` | `waiting_input` | `pending_gate` | `pending_council` | `reviewing` | `blocked` | `completed` | `failed` | `cancelled`（与 scaffold `waiting_help` 等需在实现层映射）。

## 2.6 埋点清单 — L3 Field

### Checkpoint / ResumePackage（C §5.4.6–§5.4.8）

**Checkpoint 必存七项**（fidelity 对照源）：

1. `message_thread[]`
2. `scheduling`（`SchedulingState`）
3. `se_domain_state`（含 `task_dag` 等）
4. `artifact_refs`
5. `interrupt_state`
6. `mechanical_snapshot`
7. `semantic_handoff`

**ResumePackage 必录**（`resume_success_rate`）：

| 字段                                              | 用途                                                                     |
| ------------------------------------------------- | ------------------------------------------------------------------------ |
| `restored_status`                                 | 是否进入 `running` / `ready` / `blocked` / `waiting_input` 等非 `failed` |
| `next_action`                                     | `continue` \| `wait` \| `rerun` \| `ask_human` \| `fail`                 |
| `checkpoint_id`, `resume_cursor`                  | 恢复定位                                                                 |
| `message_thread`, `scheduling`, `se_domain_state` | fidelity 对比                                                            |
| `replayed_incremental_count`                      | 增量链 replay（`IncrementalCheckpoint.deltas`）                          |

### 其他 L3

| 记录点                 | 字段                                                                    | 指标                               |
| ---------------------- | ----------------------------------------------------------------------- | ---------------------------------- |
| `SchedulingState`      | `current_turn`, `next_agent_ref`, `next_speaker_index`, `agent_order[]` | fidelity（C §5.4.4）               |
| `TaskRuntimeState`     | `waiting_on[]`, `resume_cursor`, `retry_state`                          | P2-B 阻塞恢复                      |
| `MessageDelivery`      | `status` ∈ pending/delivered/acked/timeout/failed                       | `message_deliveries_unacked_count` |
| `FileLease`            | `holder_id`, `status`                                                   | P2-B lease 临界态                  |
| `driver.tool_events[]` | `tool_name`, `status`, `created_at`                                     | N=20 门槛；duplicate_work（K=5）   |
| 锚点校验结果           | `base_commit`, `modified_files[]` hash                                  | RFC §2.3.2                         |

### resume_fidelity 对照表（kill 前 Checkpoint vs resume 后 ResumePackage）

| 对照字段                                        | C Spec 来源                |
| ----------------------------------------------- | -------------------------- |
| `message_thread` 条数                           | Checkpoint / ResumePackage |
| `scheduling.current_turn`, `next_speaker_index` | §5.4.4                     |
| `TaskRuntimeState.waiting_on`, `resume_cursor`  | §5.4.4                     |
| `restored_status`, `next_action`                | ResumePackage §5.4.8       |
| `FileLease` 持有者                              | §4.4 FileLease             |
| `MessageDelivery` 未 ack 数                     | §4.4.6                     |
| `semantic_handoff.done`, `in_progress`          | duplicate_work_rate        |

## 2.7 v0 缺口（§2）

> 更新基准：`RuntimeOrchestrator` 已对 catalog 内 EventStore 事件自动 mirror 至 `TelemetrySink`；`saveCheckpoint` 额外写入 L3 `coord.checkpoint_observed`。

| 状态                                     | 内容                                                                                                                                                                                                         |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **EventStore 已有（demo / basic-flow）** | `task.created`, `run.*`, `artifact.registered`, **checkpoint.saved**（≈ **agent.checkpoint**）, `gate.result`, `merge.authorization`, `task.completed`, `council.decision`                                   |
| **Telemetry mirror 已覆盖**              | 上述 catalog 内 L2 事件（如 `task.created`、`gate.result`、`checkpoint.saved` 等）注入 sink 后自动出现；`run.*` / `hook.*` 等非 catalog 事件**不** mirror                                                    |
| **C 必 emit 仍无（EventStore）**         | `task.claimed`, `task.started`, `task.failed`, **`task.checkpoint_resume`**, `task.before_merge`, `agent.message_*`, `system.timeout`, `system.budget_exceeded`, `lifecycle.human_gate`                      |
| **F 扩展（builder 就绪，运行时未触发）** | `eval.agent_crash`, `eval.cold_restart` — 经 `FHarnessTelemetryPort` 由外部 P2 主控 / Harness 写入，scaffold 内无 kill/resume 闭环                                                                           |
| **L3 部分**                              | `coord.checkpoint_observed` ✓（`saveCheckpoint` 时写入 TelemetrySink；payload 受 `core/checkpoint` schema 限制，**缺** `message_thread` / `scheduling` / `se_domain_state`）                                 |
| **L3 仍无**                              | `ResumePackage` 实体与 `coord.resume_package_observed` 触发；`MessageDelivery` / `FileLease` / 锚点校验；C Spec Checkpoint 七项中未入库的三项；增量 CP                                                       |
| **L1**                                   | `harness.cooperbench_evaluated` / `harness.swe_evo_evaluated` / `proxy.llm_usage_recorded` builder + port ✓；**scaffold 内无 CooperBench / P2 主控执行**                                                     |
| **schema 差距（C 方向，F 不补）**        | core `Event` 无 `subject_type`（`TelemetryRecord` 已支持）；MergeAuthorization 用 `status` 非 `authorized`；TaskStatus 枚举与 C 不一致；`CheckpointTrigger` 缺 `gate_result` / `timeout` / `budget_exceeded` |

## 2.8 附录：scaffold ↔ C Spec 快速对照

| scaffold v0                      | 对齐 C Spec                                   | 动作                               | F Telemetry 现状                                                     |
| -------------------------------- | --------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------- |
| `checkpoint.saved`               | `agent.checkpoint`                            | 重命名或双写；payload 扩至七项必存 | EventStore mirror ✓ + L3 `coord.checkpoint_observed` ✓（字段不完整） |
| `gate.result`                    | `GateResultRecord` + 可选 `task.before_merge` | 持久化 + 合并前 event              | EventStore mirror ✓                                                  |
| `merge.authorization`            | `MergeAuthorization`                          | 字段改为 `authorized`, `source`, … | EventStore mirror ✓                                                  |
| `message.*`（清单旧名）          | `agent.message_send/recv`                     | 弃用旧名                           | adapter 就绪；等 C emit                                              |
| `state.resume_*`（清单旧名）     | `task.checkpoint_resume` + L3 `ResumePackage` | 弃用旧名                           | `observeResumePackage` 就绪；等 C resume 链                          |
| `system.agent_crash`（清单旧名） | `eval.agent_crash`                            | F 扩展命名                         | builder + port ✓；等 P2 主控调用                                     |
| `src/telemetry/`                 | F 观测层                                      | 不替代 C emit                      | catalog + sink + mirror + memory-cycle observer                      |

---

## 附录：§1 / §2 交界

| 交界                | 说明                                                          |
| ------------------- | ------------------------------------------------------------- |
| `driver.run_result` | §1 看 `driver_return`；§2 看 `tool_events`                    |
| `metrics.updated`   | §1 主归属；`token_cost_total` 供 §2 P1-B                      |
| `agent.checkpoint`  | §2 专属；**不得**写入 B 记忆（C §5.4.6）                      |
| resume 判定         | §2 只看 C `ResumePackage`；§1 记忆是否「变白板」单独看 B 存储 |

---

# 三、§3 Council 互决策消融（微观内部审计）

> **硬性要求**（RFC 附录 B）：共识面控制面事件流与数据结构必须**白盒插桩**，字段需**暴露给评测上下文并持久化**（`.newide/run_audit.json`）。

## 3.0 共用定义（§3 / §4 补充）

在文首「共用定义」基础上，§3 / §4 另增：

| 层级         | 含义                                                                            |
| ------------ | ------------------------------------------------------------------------------- |
| **L2 Event** | 除 `EventStore.appendEvent()` 外，含 Council 控制面 Hook 触发点                 |
| **审计落盘** | `.newide/run_audit.json`：B.1–B.5 白盒字段的持久化出口，由 Coordinator 汇总写入 |

**依据（§3 / §4 补充）**：

- F 方向 RFC §3（Council 互决策消融）、§4（端到端打榜）、附录 A（Scaffold Evaluation Metrics）、附录 B（Ablation Audit Metrics）
- §3 事件与对象：`方向C：协调层 状态管理 与 council Spec`（Council 控制面）
- 实现锚点：`src/council/`；Hook 触发点 `src/hook/constants.ts`

**观测出口（§3 / §4 补充）**：C 侧 EventStore 事件经 `RuntimeOrchestrator.appendEvent` 自动 mirror（**仅限已在 `event-catalog.ts` 登记的 event_type**）。**不替代** C 组的 EventStore emit 与 `run_audit.json` 落盘。

**命名边界（§3 / §4 补充）**：

- `council.*` 事件名与 scaffold `src/hook/constants.ts` 中已冻结的 `CouncilHookPoint` 命名对齐；C Spec 若正式冻结 Council 事件契约，以 C 为准。
- **撞名警告**：§3 消融组名 `A1/A2/A3`（拓扑）、`B1/B2`（结构化提取）、`组①–④`（匿名化）是 **F Harness 实验配置键取值**，与 §1 记忆消融的 `B0–B3`、§2 的 `D00–D11` **无关**，不得混用。
- `.newide/run_audit.json` 是 F 对 BCD 的**白盒插桩硬性要求**（RFC 附录 B），不是 EventStore 的替代；两者字段应可互相校验。

## 3.1 评测问题与实验配置

**问题**：切掉互决策系统中某项架构设计（拓扑 / 结构化提取 / 匿名化）后，决策质量与收敛性损失多少？

| 配置键                 | 取值                                                                | 来源     |
| ---------------------- | ------------------------------------------------------------------- | -------- |
| `council.topology`     | `A1_full_mesh` / `A2_hub_spoke` / `A3_adaptive_sparse`              | RFC §3.2 |
| `council.context`      | `B1_raw_transcript` / `B2_extracted`                                | RFC §3.3 |
| `council.judge_source` | `homologous`（同源，如 GPT-4）/ `heterologous`（异源，如 DeepSeek） | RFC §3.4 |
| `council.anonymize`    | `off`（明审）/ `on`（强脱敏盲审）                                   | RFC §3.4 |
| Harness                | 同 §4 打榜集（SWE-bench Verified 裁剪 30 case）                     | RFC §3.1 |

匿名化四组矩阵 = `judge_source × anonymize`：组①（异源+明审）、组②（异源+盲审）、组③（同源+明审）、组④（同源+盲审）。

## 3.2 指标 → 埋点

| 编号    | 指标                                      | 埋点位置                     | 责任方                        | 需持久化字段                                                  | 聚合公式（F 组计算）                                              |
| ------- | ----------------------------------------- | ---------------------------- | ----------------------------- | ------------------------------------------------------------- | ----------------------------------------------------------------- |
| **B.1** | Debate Rounds（共识收敛轮数）             | `council.review_round_end`   | C 组（Council）               | `current_round_count`                                         | `Avg = Σ current_round_count_j / K`（K = 成功达成共识的 case 数） |
| **B.2** | Deadlock Rate（协议死锁率）               | Council 终止状态判定区       | C 组（Council + Coordinator） | `termination_reason`, `is_escalated`                          | `(max_rounds_exceeded 或 is_escalated=true 的任务数 / M) × 100%`  |
| **B.3** | Context Token Saving Rate（Token 节省率） | `ContextExtraction` 输出网关 | B 组 + C 组                   | `context_extraction_saved_tokens`（或 B1/B2 两组 input 总量） | `(B1 Input − B2 Input) / B1 Input × 100%`                         |
| **B.4** | Sub-model Adoption Rate（小模型采纳率）   | Decision Packet 后台审计模块 | C 组（Council）               | `identity_mapping`, `final_selected_driver`                   | `(轻量/非同源模型被采纳方案数 / 进入 Council 任务总数) × 100%`    |
| **B.5** | Evidence Score Consistency（证据一致性）  | Decision Packet 最终输出流   | C 组 + F 组（计算）           | `judge_raw_scores`, `judge_rationale_text`                    | `ρ(score, text_sentiment)`（Pearson）                             |

**辅助交叉指标**（与 §4 共用 L1，见附录三）：Pass@1、Regression Rate、Tokens per Successful Task 按消融组分桶，作为 B.1–B.5 的外部质量参照（RFC §3.1.1）。

## 3.3 埋点清单

### L2 — EventStore / Council 控制面

> `council.*` 命名已在 scaffold `src/hook/constants.ts` 作为 HookPoint 冻结；下表「状态」列为 EventStore emit / Telemetry 现状。

| event_type                     | 状态（scaffold v0）                                                                                                         | 触发                 | payload 要点                                                                                                              | 服务指标           |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| `council.started`              | Hook 常量 ✓；catalog ✓；**adapter 就绪未接线**（`observeCouncilRound`）                                                     | Council 创建         | `trigger`（`user_choice` / `agent_escalate` / `gate_defer` / `manual`）, `decision_mode`, `topology`, `participant_ids[]` | B.1 分母、B.4 分母 |
| `council.extraction_completed` | Hook 常量 ✓；catalog ✓；**adapter 就绪未接线**；ContextExtraction 模块不存在                                                | 提取器输出           | `raw_input_tokens`, `extracted_input_tokens`, `context_extraction_saved_tokens`, `context_mode`                           | B.3                |
| `council.review_round_end`     | Hook 常量 ✓；catalog ✓；**adapter 就绪未接线**（`observeCouncilRound`）                                                     | 每轮评审结束         | `current_round_count`, `round_participants[]`, `active_edges[]`（A3 拓扑剪枝审计）                                        | B.1                |
| `council.decision`             | **EventStore ✓**（`basic-flow`）；**Telemetry mirror ✓**（catalog 已登记）；payload 仍仅 `selected_proposal_id` / `verdict` | Council 终态         | 现有字段 + 需扩：`termination_reason`, `current_round_count`, `decision_packet_ref`                                       | B.1, B.2           |
| `council.completed`            | Hook 常量 ✓；catalog ✓；**adapter 就绪未接线**（`observeCouncilRound`）                                                     | Council 生命周期结束 | `duration_ms`, `total_rounds`                                                                                             | B.1 辅助           |
| `task.escalated`               | Hook 常量 ✓；catalog ✓；`TaskStatus` 含 `escalated`；**无 EventStore emit**                                                 | 升级人工介入         | `task_id`, `reason` → 同步 `is_escalated = true`                                                                          | B.2                |

**`termination_reason` 枚举（F 拟议，需 C 支持）**：`consensus` \| `max_rounds_exceeded` \| `timeout` \| `escalated`。前者计入 B.1 分母 K，后三者计入 B.2 分子。

### L3 — Decision Packet 与审计字段

| 记录点                  | 字段                                                                                                                         | 服务指标 | scaffold 对齐点                                                                                                                                     |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `decision_audit_packet` | `identity_mapping`（盲审代号 ↔ 真实模型）, `final_selected_driver`, `judge_raw_scores`, `judge_rationale_text`               | B.4, B.5 | **adapter 就绪未接线**（`observeDecisionPacket` → `audit.decision_packet_observed`）；`CouncilDecision` 仅有 `verdict` / `reason` / `evidence_refs` |
| `coordination_trace`    | `current_round_count`, `termination_reason`, `is_escalated`                                                                  | B.1, B.2 | **adapter 就绪未接线**（`observeCoordinationTrace` → `audit.coordination_trace_observed`）；`MockCouncil` 单轮直接返回，无轮次循环                  |
| `token_tracker`         | `raw_input_tokens`, `extracted_input_tokens`, `context_extraction_saved_tokens`, `total_input_tokens`, `total_output_tokens` | B.3      | **adapter 就绪未接线**（`observeTokenTracker` → `audit.token_tracker_observed`）；可与 L1 Proxy（`proxy.llm_usage_recorded`）交叉校验               |
| `Proposal` 身份扩展     | `driver_id`, `model_tier`（轻量 / 旗舰 / 同源标记）——B.4 判定的前置字段                                                      | B.4      | `council/contract.ts` 的 `Proposal` 无 driver 身份字段，**需 C 补契约**                                                                             |
| 合入链                  | `MergeAuthorization.council_decision_ref` → `selected_proposal_id`                                                           | B.4 分子 | **✓ 已有**（`core/decision.ts` + `basic-flow` `merge.authorization`）                                                                               |

### 审计落盘 — `.newide/run_audit.json` 字段映射

| JSON 区块               | 字段                                                                                               | 对应指标      | 埋点模块                                                                    | 状态（当前 scaffold）                         |
| ----------------------- | -------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------- | --------------------------------------------- |
| `coordination_trace`    | `current_round_count`                                                                              | B.1           | Council 轮次计数器                                                          | **缺失**（无轮次循环）                        |
| `coordination_trace`    | `is_escalated`                                                                                     | B.2           | Coordinator 升级状态机                                                      | **缺失**                                      |
| `coordination_trace`    | `termination_reason`                                                                               | B.2           | Council 终止判定                                                            | **缺失**；需支持枚举                          |
| `token_tracker`         | `context_extraction_saved_tokens`                                                                  | B.3           | ContextExtraction 网关                                                      | **缺失**（模块不存在）                        |
| `token_tracker`         | `total_input_tokens` / `total_output_tokens`                                                       | B.3 辅助、A.3 | L1 `proxy.llm_usage_recorded` builder ✓（含 Fair-Setup 字段）；写盘链路缺失 |
| `decision_audit_packet` | `identity_mapping`                                                                                 | B.4           | Council 双盲映射表                                                          | **缺失**                                      |
| `decision_audit_packet` | `final_selected_driver`                                                                            | B.4           | Council 决策结果                                                            | 近似 `selected_proposal_id`（无 driver 身份） |
| `decision_audit_packet` | `judge_raw_scores`                                                                                 | B.5           | Council Judge 输出                                                          | **缺失**                                      |
| `decision_audit_packet` | `judge_rationale_text`                                                                             | B.5           | Council Judge 输出                                                          | **缺失**                                      |
| 顶层                    | `council.topology` / `council.context` / `council.judge_source` / `council.anonymize` 消融配置回显 | 全部分桶      | F Harness 注入、Coordinator 回写                                            | **缺失**                                      |

## 3.4 v0 缺口（§3）

> 更新基准：`event-catalog.ts` 已登记 `council.*` / `task.escalated` / L3 `audit.*_observed`；`council.decision` 经 orchestrator mirror 已生效；`adapters/c-council.ts` adapter 就绪。

| 状态                            | 内容                                                                                                                                                                                                     |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Telemetry 已覆盖**            | `council.decision` mirror（`basic-flow` emit → `TelemetrySink`）；catalog 登记 `council.started` / `council.review_round_end` / `council.extraction_completed` / `council.completed` / `task.escalated`  |
| **EventStore 已有**             | `council.decision`（payload 仅 `selected_proposal_id` / `verdict`，缺 `termination_reason` / 轮数）；`merge.authorization` 含 `council_decision_ref` ✓                                                   |
| **adapter 就绪未接线**          | `observeCouncilRound()` / `observeDecisionPacket()` / `observeCoordinationTrace()` / `observeTokenTracker()`（`adapters/c-council.ts`）；等 C/B emit 实体后接线                                          |
| **Hook 常量已冻结、未 emit**    | `council.started` / `council.context_packaged` / `council.proposal` / `council.review` / `council.review_round_end` / `council.extraction_completed` / `council.completed`；`task.escalated`             |
| **Council 实现缺口（C 组）**    | `MockCouncil` 为单轮、恒选第一个 proposal：无辩论轮次循环（B.1）、无终止判定/升级（B.2）、无 Judge 打分与理由（B.5）、无拓扑策略注入点（`council.topology`）                                             |
| **模块不存在（B 组 + C 组）**   | `ContextExtraction` 输出网关（B.3）；双盲 `identity_mapping`（B.4）                                                                                                                                      |
| **审计落盘缺口（Coordinator）** | `.newide/run_audit.json` 与 `dumpFEvalAudit(taskId)` 汇总写盘全无                                                                                                                                        |
| **需补契约（C 方向）**          | `termination_reason` 枚举；`Proposal.driver_id / model_tier`；Decision Packet schema；`CouncilDecision.verdict`（`accept/reject/defer`）与 mock demo `verdict`（`select/needs_human/...`）不一致，需统一 |

---

# 四、§4 协作机制层端到端打榜（宏观指标）

> A.1–A.3 均在 **F 组评测框架外层**（黑盒沙箱 + Proxy）捕获，**不要求** BCD 在 `run_audit.json` 落盘；BCD 只需保证被测 Scaffold 可跑完 SWE-bench 流程。

## 4.1 评测问题与实验配置

**问题**：在底座模型硬锁定的前提下，本系统协作机制层相对竞品仿真模拟器的端到端修复质量、稳定性与经济性如何？

| 配置键                   | 取值                                                                                                   | 来源       |
| ------------------------ | ------------------------------------------------------------------------------------------------------ | ---------- |
| `bench.harness`          | SWE-bench Verified 裁剪打榜集（30 case：django 9 / scikit-learn 9 / requests 6 / flask 6）             | RFC §4.1   |
| `bench.case_tier`        | `easy` / `medium` / `hard`（各 10）                                                                    | RFC §4.1.2 |
| `bench.scaffold_variant` | `air_emulator`（仿真组①静态顺序流）/ `zcode_router`（仿真组②动态路由）/ `full_system`（实验组③本系统） | RFC §4.2   |
| 底座锁定                 | `gpt-4o-2024-05-13`；Proxy 强制 `temperature=0.2`, `max_tokens=4096`, 固定 `seed`                      | RFC §4.3   |
| 沙箱                     | `python:3.10-slim`；每 case `docker rm -f` + `git clean -fdx` 无污染重置                               | RFC §4.3   |

## 4.2 指标 → 埋点

| 编号    | 指标                          | 埋点位置                                   | 责任方              | 原始信号 / 依赖                                            | 聚合公式                                                                           |
| ------- | ----------------------------- | ------------------------------------------ | ------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| **A.1** | Pass@1（最终修复成功率）      | 评测黑盒沙箱**最外层主控脚本**             | **F 组**            | 每 case：`exit_code`、FAIL_TO_PASS 结果、PASS_TO_PASS 结果 | `Pass@1 = (成功 case 数 / M) × 100%`；成功 = ExitCode=0 且 F2P=0 且 P2P_failures=0 |
| **A.2** | Regression Rate（回归破坏率） | 沙箱内**单元测试执行器（Testbed Runner）** | **F 组**（Testbed） | 每 case：PASS_TO_PASS 是否由通过变失败                     | `(P2P 回归 case 数 / M) × 100%`                                                    |
| **A.3** | Tokens per Successful Task    | **LLM API 统一代理层（Proxy 网关）**       | **F 组**（Proxy）   | 每 case：`input_tokens`、`output_tokens`；A.1 判定结果     | `Σ(Input+Output) / Σ(1[TestCase Passed])`                                          |

## 4.3 埋点清单 — L1 Harness

| 埋点位置       | 字段                                                                                                              | 触发               | 拟议 F 观测事件名                                                       |
| -------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------ | ----------------------------------------------------------------------- |
| 主控脚本       | `case_id`, `exit_code`, `fail_to_pass_status`, `pass_to_pass_status`, `passed`, `scaffold_variant`, `case_tier`   | 每个 TestCase 结束 | `harness.swe_bench_verified_evaluated`（**builder + port ✓**）          |
| Testbed Runner | `case_id`, `pass_to_pass_regressed`（bool）, `regressed_tests[]`                                                  | 单元测试跑完       | `harness.testbed_regression_checked`（**builder + port ✓**）            |
| Proxy 网关     | `case_id`, `input_tokens`, `output_tokens`, `model`；`scaffold_variant`, `temperature`, `seed`（Fair-Setup 审计） | 每次 LLM API 调用  | `proxy.llm_usage_recorded`（**builder + port ✓**，Fair-Setup 字段已补） |
| 沙箱重置审计   | `case_id`, `container_id`, `reset_ok`（`git clean -fdx` + `docker rm -f` 完成）                                   | 每 case 开始/结束  | 可并入主控脚本记录，不必单独成 event                                    |

## 4.4 与 BCD 的边界

```text
┌─────────────────────────────────────────────────────────┐
│  F 组评测框架（黑盒外层）                                  │
│  ├─ A.1 Pass@1          ← 主控脚本                        │
│  ├─ A.2 Regression Rate ← Testbed Runner                 │
│  └─ A.3 Token 密度      ← Proxy 网关                      │
└─────────────────────────────────────────────────────────┘
                          ↑ 跑三种 scaffold_variant
┌─────────────────────────────────────────────────────────┐
│  被测 Scaffold（白盒内层，仅 full_system 开启协作层）      │
│  └─ run_audit.json ← §3 B.1–B.5 原始字段（BCD 埋点产出）  │
└─────────────────────────────────────────────────────────┘
                          ↑ F 组读取并聚合（§3 微观归因）
```

- 仿真组①②通过**硬编码剪裁协作功能**实现（①禁多轮对话与自主纠错；②Router 盲分发 + 禁长程协调与 Council），属评测侧工程，不改 scaffold 主干。
- 打榜按 `scaffold_variant` 分桶后，`full_system` 桶可与 §3 消融配置交叉，复用同一批 L1 信号。

## 4.5 v0 缺口（§4）

| 状态                 | 内容                                                                                                                                                                                  |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Telemetry 已覆盖** | `harness.swe_bench_verified_evaluated` / `harness.testbed_regression_checked` builder + port + catalog ✓；`proxy.llm_usage_recorded` 已补 `scaffold_variant` / `temperature` / `seed` |
| **评测侧工程全无**   | SWE-bench Verified 主控脚本、Testbed Runner、Docker 重置流程、Proxy 网关参数注入、30 case 裁剪脚本（冒烟排除 / 依赖排除 / 描述过滤）                                                  |
| **仿真模拟器全无**   | 仿真组①（Air 静态顺序流）、仿真组②（Z Code 动态路由）                                                                                                                                 |
| **聚合脚本未实现**   | A.1–A.3 跨 case 聚合计算在 scaffold 外（F 评测侧）；telemetry 仅提供 L1 原始信号                                                                                                      |
| **交叉校验**         | A.3 Proxy 总量 ↔ §3 `token_tracker.total_*`；A.1 判定 ↔ §3 消融分桶质量参照——两侧字段需同 `case_id` 关联                                                                              |

> §1–§4 各章「v0 缺口」均属**归因层**。结果层验收见文首 **§0.2**，勿与上表混为同一优先级。SWE-EVO smoke/dev 出分**不依赖**本节 Verified/仿真组工程。

---

## 附录（§3 / §4）：按模块拆分的埋点任务清单

### C 组 — Council / Coordinator

| 事件 / 钩子                   | 写入内容                                                                                | 服务指标 |
| ----------------------------- | --------------------------------------------------------------------------------------- | -------- |
| `council.started`             | `trigger`, `decision_mode`, `topology`, `participant_ids[]`                             | B.1, B.4 |
| `council.review_round_end`    | `current_round_count += 1`；`round_participants[]`, `active_edges[]`                    | B.1      |
| Council 终止判定              | `termination_reason`, `is_escalated`                                                    | B.2      |
| Decision Packet 生成          | `identity_mapping`, `final_selected_driver`, `judge_raw_scores`, `judge_rationale_text` | B.4, B.5 |
| `task.escalated`              | 同步 `is_escalated = true`                                                              | B.2      |
| `council.decision` 扩 payload | 现有事件补 `termination_reason`, `current_round_count`, `decision_packet_ref`           | B.1, B.2 |

### B 组 — Memory / ContextExtraction

| 事件 / 钩子                                                  | 写入内容                                                                        | 服务指标 |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------- | -------- |
| ContextExtraction 输出网关（`council.extraction_completed`） | `raw_input_tokens`, `extracted_input_tokens`, `context_extraction_saved_tokens` | B.3      |

### Coordinator — 汇总写盘

| 事件 / 钩子                | 动作                                                                      | 服务指标     |
| -------------------------- | ------------------------------------------------------------------------- | ------------ |
| checkpoint 保存 / 任务终态 | 调用 `dumpFEvalAudit(taskId)`，从 runtime 汇总写 `.newide/run_audit.json` | B.1–B.5 全量 |

### F 组 — telemetry 观测层（scaffold 内）

| 待办                                                                                                                                                | 服务指标     |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| ~~`event-catalog.ts` 登记 `council.*` / `task.escalated`~~ ✓；orchestrator mirror 已生效                                                            | B.1–B.5 观测 |
| ~~新增 adapter：`observeCouncilRound()` / `observeDecisionPacket()`~~ ✓（`adapters/c-council.ts`）；等 C/B emit 后接线                              | B.1–B.5      |
| ~~新增 L1 builder：`harness.swe_bench_verified_evaluated` / `harness.testbed_regression_checked`；`proxy.llm_usage_recorded` 补 Fair-Setup 字段~~ ✓ | A.1–A.3      |
| ~~`FHarnessTelemetryPort` 补 `recordSweBenchVerifiedEvaluation()` / `recordTestbedRegression()`~~ ✓                                                 | A.1, A.2     |

### F 组 — 评测侧（scaffold 外，不埋点只计算）

| 职责                                                                     | 服务指标         |
| ------------------------------------------------------------------------ | ---------------- |
| 读取 `run_audit.json` + L1 记录，跨 case 聚合                            | A.1–A.3, B.1–B.5 |
| B.5：`judge_rationale_text` 情感/技术点向量化 + Pearson                  | B.5              |
| B.4：按 `identity_mapping` 判定轻量/非同源模型 tier                      | B.4              |
| B.3：`council.context` B1/B2 对照组 token 差值                           | B.3              |
| 主控脚本 / Testbed Runner / Proxy 网关 / 仿真模拟器①② / 30 case 裁剪脚本 | A.1–A.3          |

## 附录（§3 / §4）：scaffold ↔ 清单快速对照

| scaffold v0 现状                                                      | 对齐清单                                                                      | 动作                                                           |
| --------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------- |
| `council.decision` event（`basic-flow` emit）                         | §3 L2 核心事件                                                                | ~~登记进 `event-catalog.ts` 使其 mirror~~ ✓；扩 payload 仍待 C |
| `CouncilHookPoint` 常量（`src/hook/constants.ts`）                    | `council.started` / `review_round_end` / `extraction_completed` / `completed` | C 组接真实 emit；F catalog ✓                                   |
| `MockCouncil`（单轮、恒选第一 proposal）                              | B.1/B.2/B.5 信号源                                                            | C 组实现轮次循环 + 终止判定 + Judge 打分；F 只观测             |
| `MockCouncilRunRequest.trigger / decision_mode`（mock demo）          | `council.started` payload                                                     | 转正式契约                                                     |
| `MergeAuthorization.council_decision_ref` ✓                           | B.4 合入链                                                                    | 已可用；补 `Proposal.driver_id / model_tier` 后闭环            |
| `TaskStatus: escalated`（`core/task.ts`）✓ / state machine 无         | B.2 `is_escalated`                                                            | C 组补 `task.escalated` emit 与状态机迁移                      |
| `proxy.llm_usage_recorded` builder + port ✓                           | A.3                                                                           | ~~补 `scaffold_variant` / `temperature` / `seed` 字段~~ ✓      |
| `harness.swe_bench_verified_evaluated` / `testbed_regression_checked` | A.1, A.2                                                                      | builder + port + catalog ✓                                     |
| `harness.swe_evo_evaluated`（SWE-EVO 专用）                           | —                                                                             | **不复用**；A.1 使用 SWE-bench Verified 专用 builder           |
| `.newide/run_audit.json`                                              | §3 审计落盘                                                                   | 全无；Coordinator 实现 `dumpFEvalAudit`                        |

## 附录：§1 / §2 / §3 / §4 交界

| 交界                       | 说明                                                                                                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------- |
| Proxy L1                   | `proxy.llm_usage_recorded` 四方共用：§1/§2 `token_per_fix`、§4 A.3、§3 B.3 交叉校验；按 `case_id` 关联  |
| `council.decision`         | §2 标注「可忽略」；§3 为**核心事件**，扩 payload 后不影响 §2 用途                                       |
| `task.escalated`           | §3 B.2 死锁分子；亦是 §2 任务生命周期事件，emit 一次两侧共用                                            |
| A.1/A.2 vs §1 Resolved/Fix | Harness 不同（SWE-bench Verified vs SWE-EVO），指标定义不互通，**勿混桶**                               |
| 消融配置键                 | `council.*` 消融与 §1 `memory.ablation`、§2 `coord.profile` 三套配置正交，run_audit / L1 记录需全部回显 |
